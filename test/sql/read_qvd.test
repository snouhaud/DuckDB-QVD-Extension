# name: test/sql/read_qvd.test
# description: Tests for the read_qvd table function
# group: [qvd]

require qvd

# A nonexistent file must make the query fail cleanly.
statement error
SELECT * FROM read_qvd('test/data/__nonexistent__.qvd');
----

# --- Tests on real data ----------------------------------------------------
# Drop a .qvd into test/data/ then uncomment and adapt:
#
# query I
# SELECT count(*) FROM read_qvd('test/data/example.qvd');
# ----
# 42
#
# query II
# SELECT id, name FROM read_qvd('test/data/example.qvd') ORDER BY id LIMIT 1;
# ----
# 1	alpha
